# Basket

The basket, held on the server so it survives a reload and follows the shopper into their account. Lines warn when stock has moved beneath them; checkout stays blocked until that is resolved.
