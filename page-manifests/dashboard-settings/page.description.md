# Shop settings

Read-only on purpose. Everything shown comes from brand.generated.json and site-content.json, which the builder owns; a form here would create a second source of truth the next build overwrites.
