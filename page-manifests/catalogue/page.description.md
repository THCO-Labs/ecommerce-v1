# Product catalogue

Every published product, filtered by category and searched with ?q=. Filters are links rather than client state so a filtered catalogue stays a URL a shopper can share. Cards here are denser than the homepage's because the grid shows many at once.
